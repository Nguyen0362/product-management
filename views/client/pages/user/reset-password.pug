extends ../../layouts/default.pug

block main
  .container.my-5
    .row.justify-content-center
      .col-8
        h1(class="text-center") #{pageTitle}
        form(
          action="/user/password/reset"
          method="POST"
        )
          div(class="form-group")
            label(for="pasword") Mật khẩu mới
            input(
              type="text"
              class="form-control"
              id="pasword"
              name="password"
              required
            )
          div(class="form-group")
            button(
              type="submit"
              class="btn btn-primary btn-block"
            ) Đổi mật khẩu
